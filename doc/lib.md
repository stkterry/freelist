[`Freelist`] uses a [`Vec`] under the hood as opposed to a linked-list.  As such, it's almost as performant when inserting and removing elements.

This library is performant and quite usable, but not entirely feature complete.

